#ifndef _CORE_UTILS_TRIM_FILENAME_H
#define _CORE_UTILS_TRIM_FILENAME_H 1

#include "split.h"
/**
 * Corta o caminho do arquivo reduzindo-o a somente o
 * nome do arquivo */
const char* trim_filename(const char *filename);

#endif